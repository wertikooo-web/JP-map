(()=>{const cache=new Map();async function add(){const card=document.querySelector('.card');if(!card||card.querySelector('.kando-photo'))return;const title=card.querySelector('h2')?.textContent?.trim();if(!title)return;const w=document.createElement('div');w.className='kando-photo kando-photo-loading';w.innerHTML='<div class="kando-photo-skeleton"></div>';card.insertBefore(w,card.firstChild);try{let d=cache.get(title);if(!d){const r=await fetch('/.netlify/functions/place-photo?place='+encodeURIComponent(title));if(r.ok)d=await r.json();cache.set(title,d||{})}if(d?.url){w.className='kando-photo';w.innerHTML=`<img src="${d.url}" alt="${d.alt||title}" loading="lazy"><a href="${d.photographerUrl||d.sourceUrl||'#'}" target="_blank" rel="noopener">Photo: ${d.photographer||'Unsplash'} / Unsplash</a>`}else w.remove()}catch(e){w.remove()}}new MutationObserver(add).observe(document.documentElement,{childList:true,subtree:true});add()})();